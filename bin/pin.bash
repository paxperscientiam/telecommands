#!/bin/bash

# NOHUP is good for long running processes that dont need interaction
# nohup <command> &
# or...
# use screen for more complex management including stuff that needs interaction
# screen -m -d -S screenname <command>
# screen -list
# screen -r screenname
